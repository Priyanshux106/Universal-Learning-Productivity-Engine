# StuddyBuddy AI — Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# The base URL of your deployed AWS API Gateway endpoint
# After running `sam deploy`, find the API endpoint in the SAM output
# Example: https://abc123xyz.execute-api.us-east-1.amazonaws.com
NEXT_PUBLIC_API_URL=https://YOUR_API_GATEWAY_URL_HERE

# (Optional) Your Identity Provider / Cognito User Pool info
# If using AWS Cognito, these are needed for the auth flow
NEXT_PUBLIC_COGNITO_REGION=us-east-1
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
NEXT_PUBLIC_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
